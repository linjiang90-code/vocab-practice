# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# OS / editor temp
.DS_Store
Thumbs.db
*.bak
*.tmp
*~

# WorkBuddy runtime (project-local, never track)
.workbuddy/

# Auth secret (NEVER commit — repo is public on GitHub!)
serve_auth.json

# Runtime log (append-only server output)
serve.log
